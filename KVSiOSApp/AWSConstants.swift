import Foundation

/// AWS认证相关常量
struct AWSConstants {
    /// AWS访问密钥ID
    static let ACCESS_KEY = "replace"
    
    /// AWS秘密访问密钥
    static let SECRET_KEY = "replace"
    
    /// AWS会话令牌
    static let SESSION_TOKEN = "replace"
    
    /// 凭证过期时间（秒）
    static let CREDENTIALS_DURATION: TimeInterval = 6000
} 
